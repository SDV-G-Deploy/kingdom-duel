# Result Terminal Event Pass

Date: 2026-06-29

## Intent

Polish the bottom terminal event strip so it stops feeling like a generic utility row once the duel is over.

## Scope

- add outcome-specific terminal copy for the bottom event strip
- add a compact terminal badge that echoes the current result state
- retune the terminal strip CTA so it reads like a post-duel action instead of a debug button

## Constraints

- no board changes
- no new interaction model
- keep the strip within current mobile layout bounds
