# Kingdom Duel Night Run

Date: 2026-06-28

Session: `kingdom-duel-night-2026-06-28`

## Objective

Run 15 bounded autonomous passes that make Kingdom Duel more playable, legible, distinctive, and RPG-readable. Avoid nominal churn. Each pass must either land a useful improvement with verification or stop with a clear reason.

## Baseline

- Latest pre-night gameplay fix: `316ffec Improve mobile swap targeting`
- Public URL: https://sdv-g-deploy.github.io/kingdom-duel/
- Current focus: mobile match-3 RPG duel, AeroCandy 2007 visual identity, stronger tactical clarity.

## Hard Rules

- One pass, one bounded useful step.
- Acquire `tmp/night-runs/2026-06-28.lock` before editing.
- If the repo has unexpected dirty state, stop and write a blocked entry.
- Do not overwrite user changes.
- Prefer code and playable improvements over docs-only work, except when research or roadmap synthesis is the pass objective.
- Run the smallest meaningful verification for each pass. At minimum:
  - `npm test`
  - `npm run build`
  - `git diff --check`
- Commit only when verification passes.
- Push only attributable, verified commits.
- If repeated passes become low-value, stop early and write why.

## Pass Backlog

1. Verify the mobile swap fix against the exact reported case; add a small UX note or testable helper if needed.
2. Improve selected-tile affordance and valid-target readability on mobile.
3. Tighten decision preview copy and make invalid/snap-back feedback clearer.
4. Strengthen enemy intent readability: show why Shade wants the highlighted move.
5. Review Puzzle Quest / Gems of War / RPG match-3 references and extract only mechanics that fit Kingdom Duel.
6. Add one small gameplay improvement from the research, preferably tactical rather than cosmetic.
7. Improve spell readability and targeting labels for Sun Bloom, Glass Ward, Crown Strike.
8. Tune shade/backlash communication so the danger tile is understandable before it hurts the player.
9. Improve RPG combat language: HP, guard, mana, damage, extra turn, enemy action.
10. Visual identity pass: make Aurora vs Shade, sun/water/glass vs violet-danger, more recognizable without clutter.
11. Board readability pass: gem silhouettes, rings, selected/preview/threat states, mobile 390px sanity.
12. Add or update screenshots for the most important changed mobile/desktop states.
13. Update roadmap with landed improvements and the next 3-5 meaningful milestones.
14. Run full verification, public URL/asset checks if deployed, and inspect git/CI state.
15. Closeout: write summary, unresolved risks, next recommended task, and morning report.

## Ledger

Use the table below. Append one row per pass.

| Pass | Status | Commit | Verification | Notes | Next |
| --- | --- | --- | --- | --- | --- |
| 0 | ready | `316ffec` | `npm test`, `npm run build`, `git diff --check` passed before scheduling | Mobile swap targeting fix pushed before night run. | Start pass 1. |
| 1 | landed | `bbdbd7c` | `npm test`, `npm run build`, `git diff --check` passed | Verified the mobile swap fix by extracting drag target choice into a pure helper and adding a regression test for invalid directional swipe plus valid release cell. | Pass 2: improve selected-tile affordance and valid-target readability on mobile. |
| 2 | landed | `a1c2f24` | `npm test`, `npm run build`, `git diff --check` passed; Chromium 390x844 smoke screenshot rendered | Improved mobile board affordance: selected tiles now have a stronger yellow anchor ring, valid targets get a distinct aqua bullseye marker, and non-matching neighbors are quieter. | Pass 3: tighten decision preview copy and invalid/snap-back feedback. |
